[user]
    email = giovanim@ymail.com
    name = Giovani Moutinho
[core]
    editor = vim
[diff]
    tool = vimdiff
[alias]
    slog = log --all --graph --pretty=format:'%Cred%h%Creset -%C(auto)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
[color]
    ui = true
[gpg]
    program = gpg
[commit]
    gpgsign = true
[merge]
    tool = vimdiff
[mergetool "vimdiff"]
    cmd = vim -d "$LOCAL" "$MERGED" "$REMOTE"
[push]
    default = simple
[grep]
    break = true
    heading = true
    lineNumber = true